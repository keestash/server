<!DOCTYPE html>
<html lang="{{ language }}">
{{ head|raw }}
{{ body|raw }}
</html>


<data-node
        id="data-node"
        data-api-host="{{ apiHost }}"
        data-host="{{ host }}"
        data-breadcrumb-template="{{ breadcrumbTemplate }}"
>
</data-node>