<!DOCTYPE html>
<html lang="{{ language }}">
{{ head|raw }}
{{ body|raw }}
</html>


<data-node
        id="data-node"
        data-api-host="{{ apiHost }}"
        data-host="{{ host }}"
        data-part-template="{{ partTemplate }}"
        data-side-bar-template="{{ sidebarTemplate }}"
        data-breadcrumb-template="{{ breadcrumbTemplate }}"
>
</data-node>