<!DOCTYPE html>
<html class="" lang="{{ language }}">
{{ head|raw }}
{{ body|raw }}
</html>

<data-node
        id="data-node"
        data-api-host="{{ apiHost }}"
        data-host="{{ host }}"
>
</data-node>