<!DOCTYPE html>
<html class="d-flex flex-grow-1" lang="{{ language }}">
{{ head|raw }}
{{ body|raw }}
</html>

<data-node
        id="data-node"
        data-api-host="{{ apiHost }}"
        data-host="{{ host }}"
>
</data-node>