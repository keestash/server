# my global config
global:
  scrape_interval: 10m # Set the scrape interval to every 15 seconds. Default is every 1 minute.
  evaluation_interval: 10m # Evaluate rules every 15 seconds. The default is every 1 minute.
  # scrape_timeout is set to the global default (10s).

# Alertmanager configuration
alerting:
  alertmanagers:
    - static_configs:
        - targets:
          # - alertmanager:9093

# Load rules once and periodically evaluate them according to the global 'evaluation_interval'.
rule_files:
# - "first_rules.yml"
# - "second_rules.yml"

# A scrape configuration containing exactly one endpoint to scrape:
# Here it's Prometheus itself.
scrape_configs:
  - job_name: "keestash"
    scrape_interval: 5s
    scheme: https  # Use HTTPS instead of HTTP
    bearer_token: eyJ0eXAiOiJKV1QiLCJhbGciOiJIUzI1NiJ9.eyJpc3MiOiIiLCJhdWQiOnsidHlwZSI6ImFwaS50eXBlIiwidmFsdWUiOiJjZjhlNTJmNS1jMmRjLTQ3MDEtYTBjMi0xMTljYWI4ZDM2YjcifSwiaWF0IjoxNzA5NjU3NDYwLCJuYmYiOjE3MDk2NTc0NTl9.v2tfq5wDDG6ab0exot-UebcCQrRwXu8sVJ1C9lxLay8
    metrics_path: /api.php/metrics
    static_configs:
      - targets: ["keestash-web"]
    tls_config:
      insecure_skip_verify: true
